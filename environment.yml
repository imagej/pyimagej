name: imagej
channels:
  - conda-forge
dependencies:
  - imglyb=0.3.5
  - matplotlib
  - numpy
  - openjdk=8
  - scyjava
  - pillow # for server
  - requests # for server
  - xarray
  - pip:
    - pyjnius
